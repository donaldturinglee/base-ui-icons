{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", "lib/**"]
    },
    "typecheck": {
      "dependsOn": ["build"]
    }
  }
}
